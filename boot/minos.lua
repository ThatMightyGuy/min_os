do
    -- kernel space
    local kernel = {}
    do
        local processes = {}
 
        -- Move this elsewhere.
        -- Returns table: stream, string error with read, write, seek methods
        function kernel.open(path, mode)
            mode = mode or "r"

        end
    end
    -- user space
    
end

