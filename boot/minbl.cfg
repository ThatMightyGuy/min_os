{
    timeout=5,
    default=1,
    mode=0,     -- 0: visible
                -- 1: hidden until key press, else boot default
                -- 2: skip and load last booted
                -- 3: skip and load default
    bootEntries = {
        {name="min_os", address=nil, path="/boot/minos.lua", args={"base", "io", "tty", "fstab"}}
    }
}